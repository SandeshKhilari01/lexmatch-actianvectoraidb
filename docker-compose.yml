version: '3.8'
services:
  vectoraidb:
    image: williamimoh/actian-vectorai-db:latest
    container_name: lexmatch-vectordb
    ports:
      - "50051:50051"
    volumes:
      - ./data:/data
    restart: unless-stopped
